#ifndef __LIST_H__
#define __LIST_H__
#include "stdint.h"
//
//????????????????????????????????????????
//
//?????????,????????,?????????????-1


enum TaskState{
    TASK_RUNNING,
    TASK_READY,
    TASK_WAITTING,
    TASK_DELAY,
    TASK_BLOCK,
    TASK_HANGING            
};



typedef struct Node{
    struct Node* next;
}Node;

struct List{
    char*listName;
    Node* head;
    Node* tail;
    
};


struct Task{
    Node* allListNode;
    Node* ReadyListNode;
    Node* BlockListNode;
    uint8_t* stack_ptr;
    uint8_t* Stacksize;
    uint8_t prio;
    uint8_t tickTime;
    uint8_t delayTick;
    uint8_t status;
};

//???????
extern xdata struct List AllList;
//??????????????,??????????

//???????
extern xdata struct List ReadyList;
//????????????,??????????

//?????????
extern xdata struct List BlockList;
//????????????,???????????


#define NULL 0


#define elem2entry(struct_type,member_name,ptr_to_member) \
    (struct_type*)((uint8_t*)(ptr_to_member)-(uint8_t*)(&((struct_type*)0)->member_name))



typedef BOOL (function)(struct Node* node,int arg);

void listInit(struct List* list,char* ListName);
void listAppend(struct List* list,Node* node);
struct Node* listPop(struct List* list);
void listPush(struct List* list,struct Node* node);
BOOL ListEmpty(struct List* plist);
struct Node* listRemove(struct List*,Node* node);

void SystemListInit();
#endif